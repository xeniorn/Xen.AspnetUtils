<Project Sdk="Microsoft.NET.Sdk">

	<PropertyGroup>
		<LangVersion>latest</LangVersion>
		<TargetFrameworks>net8.0;net9.0;net10.0</TargetFrameworks>
		<ImplicitUsings>enable</ImplicitUsings>
		<Nullable>enable</Nullable>
		<BaseOutputPath>bin\</BaseOutputPath>
		<VersionPrefix>0.0.0</VersionPrefix>
		<VersionSuffix></VersionSuffix>
	</PropertyGroup>

	<Choose>
		<When Condition=" '$(VersionSuffix)'!='' ">
			<PropertyGroup>
				<PackageVersion>$(VersionPrefix)-$(VersionSuffix)</PackageVersion>
			</PropertyGroup>
		</When>
		<When Condition=" '$(VersionSuffix)'=='' ">
			<PropertyGroup>
				<PackageVersion>$(VersionPrefix)</PackageVersion>
			</PropertyGroup>
		</When>
	</Choose>

	<PropertyGroup>
		<PackageId>Xen.Utils.AspnetAuthorizationHelper</PackageId>
		<Authors>Juraj Ahel</Authors>
		<Description>For strongly-typed policy-based auth in aspnet, conforming to an app-internal security standard</Description>
		<PackageProjectUrl>https://github.com/xeniorn/Xen.AspnetUtils</PackageProjectUrl>
		<RepositoryUrl>https://github.com/xeniorn/Xen.AspnetUtils</RepositoryUrl>
		<PackageIcon>icon.png</PackageIcon>
		<PackageTags>api;security;authentication;authorization</PackageTags>
		<PackageReadmeFile>README.md</PackageReadmeFile>
		<PackageLicenseFile>LICENSE</PackageLicenseFile>
	</PropertyGroup>

	<ItemGroup>
		<None Include="../README.md" Pack="true" PackagePath="" />
		<None Include="../LICENSE" Pack="true" PackagePath="" />
		<None Include="../Resources/icon.png" Pack="true" PackagePath="" />
	</ItemGroup>

	<ItemGroup>
		<PackageReference Include="Microsoft.AspNetCore.Authentication" Version="2.3.9" />
		<PackageReference Include="Microsoft.AspNetCore.Authorization" Version="9.0.1" />
		<PackageReference Include="Microsoft.Extensions.Hosting.Abstractions" Version="9.0.1" />
	</ItemGroup>

</Project>
